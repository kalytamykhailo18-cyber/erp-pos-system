# Scale Bridge Configuration - PRODUCTION MODE
# Multi-branch mode - handles ALL branches dynamically
# BRANCH_ID is NOT required

# Backend URL (production)
BACKEND_URL=https://api.grettas-erp.com

# Logging level (debug, info, warn, error)
LOG_LEVEL=info

# Connection timeouts (milliseconds) - PRODUCTION VALUES
# Delay before first reconnection attempt
RECONNECTION_DELAY=5000

# Maximum delay between reconnection attempts
RECONNECTION_DELAY_MAX=60000

# How many times to try reconnecting (999 = almost unlimited)
MAX_RECONNECTION_ATTEMPTS=999

# How long to wait for scale operations (RS-232 can be slow)
REQUEST_TIMEOUT=120000
